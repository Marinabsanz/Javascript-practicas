'use strict'
  //calcular área de un circulo
const randomNumber = Math.random();
const PI= Math.PI;
//let area= PI+radio*radio;



//ahora, condicionales .definimos con llaves bloques de codigo
if (randomNumber <=0.5) { 
  console.log ('SI');

} else {  (randomNumber >= 0.8)
console.log ('NO');
}