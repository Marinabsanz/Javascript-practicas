//más métodos para objetos
const product = {
  nombre: 'monitor de 20"',
  precio: 300,
  disponible: true,
};

console.log(Object.keys(product)); //ver que tiene, o comprobar si obj está Vavío part izq

console.log(Object.values(product)); //ver valores. part derecha

console.log(Object.entries(product)); //todo en pares
